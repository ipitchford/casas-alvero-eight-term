# Independent review of the direct unit-16 obstruction

Reviewed source: ../boundary/DIRECT_JET_THEOREM.md.

Result: the stated local unit-16 exclusion passes this mathematical review and the independent integer-identity replay. This is an internal argument audit, not external refereeing or formal verification.

The exact hypotheses are important: a2=a18=0; a4 through a15 reduce to zero; a16 reduces to -1; the row-1 seed is X20-X3; the mean is exactly simple. All coefficients and roots are integrally normalized. No hypothesis that a3 vanishes is necessary.

The initial small-root estimates hold with arbitrary finite ramification. Since D is a unit and all other low ordinary coefficients have at least their binomial factor 17, the two nonzero roots in the zero residue cluster have value delta=nu(E)/2. The equation f'(q)-f(q)/q has its unique lowest term 2Dq^2, excluding repeated roots there. When a3=t is nonzero, G3=X^3+t makes nu(t)=3delta; the formula for E then yields delta>=1/2. The first divided E residue is 11, so delta=1/2. Induction along the normalized derivatives gives nu(a_j)>=j/2 for 4<=j<=15 because their selected witnesses cannot be units. Thus every omitted ordinary middle-coefficient contribution has value at least three, and the omitted contribution to the G16 formula has value at least two. Multiplication of the latter by the ordinary coefficient 4845 restores the third-order error bound in Q.

The collision argument covers all possible G16 witness displacements, including exact x=1. For t=0 and 0<nu(x-1)<1, substituting the G16 equation makes the two potentially minimal terms in f(x)/(x-1) have coefficients 10 and 1 and values 1+epsilon and 16epsilon, so epsilon=1/15. For epsilon>=1 the fifteen outer roots are simple and the inner cluster has multiplicity two; it contains one and x. Every repeated root must lie there, forcing x to be the exact double root one. For t nonzero the same balance holds below epsilon=1/2. Above that value, including x=1, the surviving t contribution gives c1 value 3/2 and rules out a repeated root. At epsilon=1/2, the equation itself forces c1 value 3/2, producing fifteen simple outer roots, a simple root one and a single simple inner root. This also cannot be a CA polynomial.

In the only remaining displacement case, epsilon=1/15, the full leading cluster polynomial is Y17+7Y^2+3xiY with xi^15=7. Its derivative has the unique zero xi, and the corresponding multiple root has multiplicity exactly two. All seventeen roots are accounted for in this leading cluster. The selected G16 root already lies in that double cluster; an exact repeated root must occupy the same cluster. Its two multiplicities exhaust the cluster, so the selected root is itself repeated. This justifies imposing f'(x)=0 only after the collision proof.

The adjacent independent program check_direct_jet_independent.py reconstructs Q=f'-f/X directly by coefficient contributions and the G16 substitution. It obtains the stated Q0 and Q1, expands Q0 at one using integer binomial coefficients, and confirms that only z^2 and z^17 attain value 17/15 when nu(z)=1/15. Their combined residue is xi^2(1+2xi^15)=15xi^2, nonzero. Consequently Q0(x) has value exactly 17/15, below both the possible tQ1 term (at least 3/2) and the discarded error (at least three). The exact x=1,t=0 case has Q0(1)=21964 of value two, also below the error. These are genuine strict inequalities; cancellation by a ramified omitted term cannot occur.

Normal and optimized standard-library replays pass and produce identical direct-jet-verification.json receipts. The mathematical conclusion is the whole stated unit-16 residue stratum, including t=0 and nonzero a4. It does not by itself cover arbitrary mixed middle residues or arbitrary degree-twenty support.
