# Files before the portability revision

These ten files are exact copies from `outputs/casas-alvero-review` before the
portable revision. The original package is unchanged. The old root and nested
manifests are historical snapshots: to verify an old entry, use its file under
this directory when present, and the unchanged current file otherwise.

The older `evidence/full/collective17/exclusions/MANIFEST.before-m6.json` also
retains its original REPORT/COVERAGE mapping to the corresponding
`batch-m6/*.before-m6.*` files. Its old `check_batch.py` entry now resolves to
this directory's pre-portability copy. No historical manifest is described as
the current package manifest.
